# Configuration Django
DEBUG=False
SECRET_KEY=GENERER-UNE-CLE-SECRETE-LONGUE-ET-ALEATOIRE-CHANGEZ-MOI
ALLOWED_HOSTS=76.13.56.224,localhost,127.0.0.1

# ============================================
# Configuration SQLite (Pour tester d'abord)
# ============================================
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3

# ============================================
# Configuration PostgreSQL (Pour production)
# ============================================
# Décommentez ces lignes et commentez SQLite pour utiliser PostgreSQL
# DB_ENGINE=django.db.backends.postgresql
# DB_NAME=servicemenager
# DB_USER=postgres
# DB_PASSWORD=VOTRE-MOT-DE-PASSE-FORT-CHANGEZ-MOI
# DB_HOST=db
# DB_PORT=5432

# Configuration CORS
CORS_ALLOWED_ORIGINS=http://76.13.56.224
CORS_ALLOW_ALL_ORIGINS=False

# URL de l'API pour le frontend
VITE_API_URL=http://76.13.56.224/api

# Configuration des fichiers statiques (Docker gère cela)
STATIC_ROOT=/app/staticfiles
MEDIA_ROOT=/app/media

# Configuration Email (optionnel)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=noreply@serviceslocaux.ci
